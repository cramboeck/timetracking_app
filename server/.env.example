# Server Configuration
PORT=3001
NODE_ENV=development

# Database (PostgreSQL)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://ramboflow:your_password@localhost:5432/ramboflow

# JWT Secret (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ===========================================
# Email Configuration
# ===========================================

# Email Provider: "smtp", "graph", or "auto" (auto tries Graph first, then SMTP)
EMAIL_PROVIDER=auto

# Test Mode (redirects all emails to test recipient)
EMAIL_TEST_MODE=true
EMAIL_TEST_RECIPIENT=your-email@example.com

# ----- Option 1: SMTP (e.g., Gmail, M365 SMTP) -----
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM="TimeTrack App <noreply@timetrack.app>"

# For Microsoft 365 SMTP:
# EMAIL_HOST=smtp.office365.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@yourdomain.com
# EMAIL_PASSWORD=your-app-password

# ----- Option 2: Microsoft Graph API (recommended for M365) -----
# See docs/AZURE_SETUP.md for Azure App Registration instructions
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# GRAPH_MAIL_FROM=noreply@yourdomain.com

# Future: Mailbox Monitoring (for ticket creation from emails)
# GRAPH_SUPPORT_MAILBOX=support@yourdomain.com
# GRAPH_INVOICE_MAILBOX=rechnung@yourdomain.com

# Notification Settings
NOTIFICATIONS_ENABLED=true
NOTIFICATION_CHECK_INTERVAL="0 */1 * * *"  # Every hour
